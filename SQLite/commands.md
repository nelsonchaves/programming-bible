# SQLite Commands

```ruby
Rails db

.tables - lists all the tables

INSERT INTO users (name, email, bio, age, happy, created_at, updated_at) VALUES ("John", "john@example.com", "", 25, true, "2022-01-01", "2022-01-01");

.mode column - display nice
select * from users;
```
